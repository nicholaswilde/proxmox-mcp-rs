# Proxmox MCP Server Configuration Example
# Copy this to 'config.toml' in the same directory as the binary

# Proxmox Host (IP or Domain)
host = "192.168.1.10"

# Proxmox Port (Default is 8006)
port = 8006

# Proxmox User (Realm is required, e.g. @pam or @pve)
user = "root@pam"

# Authentication: Use EITHER password OR token_name + token_value

# 1. Password Authentication
# password = "your-password-here"

# 2. API Token Authentication
# Token Name (Token ID)
# token_name = "mytoken"
# Token Value (Secret/UUID)
# token_value = "XXXXXXXX-XXXX-XXXX-XXXX-XXXXXXXXXXXX"

# Disable SSL Verification (useful for self-signed certificates common in Proxmox)
# Default is false (verification enabled)
# no_verify_ssl = true

# --- Logging Configuration ---

# Log level: error, warn, info, debug, trace (Default: info)
# log_level = "info"

# Enable logging to a file (Default: false)
# log_file_enable = false

# Directory for log files (Default: .)
# log_dir = "."

# Log filename prefix (Default: proxmox-mcp-rs.log)
# log_filename = "proxmox-mcp-rs.log"

# Log rotation strategy: daily, hourly, never (Default: daily)
# log_rotate = "daily"
